# Baseline Snapshot — servalws, 2026-08

Frozen record of the live system state at Phase 0 completion. Captured
2026-08-18, all read-only (`configure-host.sh doctor`, `find`, `ls`,
version queries; no mutations, no git). Later phases compare against this.

## Host & tool versions

| Component | Version |
|---|---|
| Host | servalws (primary authoring host) |
| OS | Fedora Linux 44 (Workstation), kernel 7.1.5-201.fc44.x86_64 |
| GNU Stow | **2.4.1** (repo docs/bootstrap pin 2.3.1 — known skew, see analysis/01) |
| chezmoi | **v2.72.0 already installed** (Fedora build, commit f81cb321) — Phase 1 needs no binary install |
| age | **not installed** — required for spike U2 (ssh encryption); needs one dnf install |
| gpg | 2.4.9 |
| podman / docker | 5.8.4 / 29.7.1 (both present; U7 rehearsal uses podman rootless) |

## Live symlink census (stow farm)

`find ~ -maxdepth 4 -xdev -type l -lname '*dotfiles*'` → **130 symlinks,
0 broken.**

| Target area | Count |
|---|---|
| ~/.config/* | 79 |
| ~/.local/* (my-bin) | 23 |
| ~/.tmux/* | 15 |
| ~/.ssh/* | 2 (config, authorized_keys → `ssh/servalws/.ssh/`) |
| dotfiles (~/.zshrc, ~/.bashrc, ~/.gitconfig, ~/.vimrc, ~/.tmux.conf, ~/.zprofile, ~/.bash_profile, ~/.p10k.zsh, ~/.zsh) | 9 |
| odd top-level links | 2: `~/README.md` → `.dotfiles/quickshell/README.md`; `~/vimium` → `.dotfiles/scripts/vimium` |

`~/.ssh` also holds real files that are NOT stow-managed and must survive
migration untouched: `known_hosts`, `known_hosts.old`, `agent` (socket),
`config.bak-20260615-154030`, `config.bak-20260615-154520`.

## As-deployed profile (servalws)

- Common packages: zsh bash git vim nvim delta my-bin tmux-remote
- Extra: apps 1Password wezterm flameshot awesome quickshell
  awesome_wm_scripts picom awesomewm-bin
- Overlays: tmux `tmux-remote-servalws`, ssh `servalws`
- Theme: orange-gas-plasma · Toolset: desktop-full · OS allow: fedora
- Modules (8): system:xorg-libinput, system:1password-unlock-polkit,
  system:ly-display-manager, system:ly-screen, system:mate-polkit-package,
  system:battery-charge-thresholds, user:gnome-keyring-units,
  user:awesome-auth-startup

## configure-host doctor (2026-08-18) — summary

- Catalog vs disk: all 30 catalog packages present; missing_on_disk=0.
- Disk vs catalog: 5 archive dirs (expected); 1 package-like dir
  `stow-to-chezmoi-migration` (this project — expected, ignore).
- Profile vs catalog: 18 registered, 0 mismatches.
- Tree-fold safety: `.local`, `.local/state`, `.local/share`, `.tmux` all
  real directories (no folding) — the guard held.
- Syncthing: repo detected as Syncthing-managed (.stfolder present).
- tpm present (tmux plugins applied outside configure-host).

## Revert invariant (the standing fallback)

Until Phase 5 completes, any host can revert to full stow management by
re-running: `bash ~/.dotfiles/scripts/configure-host.sh apply`
(the old system stays installed and functional throughout).

## Post-migration comparison checklist

1. `chezmoi verify` exit 0 and `chezmoi status` clean on servalws.
2. Stow-farm census re-run: dangling `*dotfiles*` links = 0 after Seq A/B
   sweep; the 130 real files/templated outputs now chezmoi-managed.
3. `~/.ssh` real-file set (known_hosts, agent, .bak files) unchanged.
4. Apps smoke: wezterm, awesome, quickshell, tmux load as before.
5. This baseline retained for diffing if anything looks off.
