CLAUDE.md
nvim/.config/nvim/lazy-lock.json
.stfolder

/.ssh/

# SSH operational data (config is tracked, known_hosts are not)
ssh/**/.ssh/known_hosts

# Syncthing conflict files
*.sync-conflict-*

1Password/

# Python bytecode cache
__pycache__/
