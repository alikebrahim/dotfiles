# Uncommitted changes — 2026-05-30

## Modified tracked files

- `awesome/.config/awesome/dynamism.lua` — Scratchpad now opens WezTerm running Neovim in insert mode against a timestamped `/tmp/file...` note.
- `awesome/.config/awesome/keys.lua` — Reworked spatial focus helpers and made previous/next workspace navigation shift all screens together while preserving focus.
- `my-bin/.local/bin/note` — Improved fzf note previews and content-search preview behavior.
- `nvim/.config/nvim/lua/config/options.lua` — Added remote OSC52 clipboard provider for SSH sessions; keeps `unnamedplus` yanks seamless.
- `wezterm/.config/wezterm/modules/appearance.lua` — Added retro tab-bar theme support, adjusted opacity, tab width, and selected terminal color scheme.
- `wezterm/.config/wezterm/modules/keybindings.lua` — Added leader-comma tab rename prompt.
- `wezterm/.config/wezterm/modules/status.lua` — Rebuilt classic text-based tab/status formatting using retro theme palette; removed Nerd Font icon status style.
- `zsh/.zshrc` — Added WezTerm-aware `ssh()` wrapper for temporary SSH tab titles; added Grok installer PATH/completion block.

## Untracked files/directories

- `awesome/.config/awesome/dynamism.lua` — Scratchpad now opens WezTerm running Neovim in insert mode against a timestamped `/tmp/file...` note.
- `awesome/.config/awesome/keys.lua` — Reworked spatial focus helpers and made previous/next workspace navigation shift all screens together while preserving focus.
- `my-bin/.local/bin/note` — Improved fzf note previews and content-search preview behavior.
- `nvim/.config/nvim/lua/config/options.lua` — Added remote OSC52 clipboard provider for SSH sessions; keeps `unnamedplus` yanks seamless.
- `wezterm/.config/wezterm/modules/appearance.lua` — Added retro tab-bar theme support, adjusted opacity, tab width, and selected terminal color scheme.
- `wezterm/.config/wezterm/modules/keybindings.lua` — Added leader-comma tab rename prompt.
- `wezterm/.config/wezterm/modules/status.lua` — Rebuilt classic text-based tab/status formatting using retro theme palette; removed Nerd Font icon status style.
- `zsh/.zshrc` — Added WezTerm-aware `ssh()` wrapper for temporary SSH tab titles; added Grok installer PATH/completion block.
- `quickshell/` — New Quickshell config package with QML shell, theme tokens, and README.
- `static/theme.conf` — Machine-local tmux theme selector/static theme file.
- `static/vomnibar_catppuccin-frappe-v2.css` — New Vimium Vomnibar Catppuccin Frappe theme.
- `static/vomnibar_kanagawa-wave-v2.css` — New Vimium Vomnibar Kanagawa Wave theme.
- `tmux-pi/` — New remote tmux package, including OSC52 clipboard support, tmux persistence plugins, themes, and tmux-fzf setup.
- `vim/.vimrc` — New lightweight Vim config with Vim 9.1+ OSC52 yank support.
- `wezterm/.config/wezterm/\\` — Stray untracked file with backslash name; likely accidental.
- `wezterm/.config/wezterm/modules/retro_themes.lua` — New retro color palette module for WezTerm tab/status styling.
- `zsh/.zprofile` — New zsh profile file.
- `changes(2026-05-30).md` — This summary file.
