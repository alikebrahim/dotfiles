# Quickshell Work Area

This directory is for a greenfield AwesomeWM/X11-native Quickshell companion shell.

- `.config/` is the clean active prototype implementation.
- `reference-dms/` preserves the previous DankMaterialShell-based attempt for reference only.
- Do not port DMS wholesale into `.config/`.
- Do not autostart Quickshell or replace Polybar/Rofi/Dunst until a component is proven manually.
